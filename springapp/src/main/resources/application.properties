# application-prod.properties (updated)
# ==========================================
# PRODUCTION CONFIGURATION FOR RENDER
# ==========================================

# PostgreSQL Database
spring.datasource.url=jdbc:postgresql://dpg-d46qi51r0fns73bdked0-a.oregon-postgres.render.com:5432/todoapp_db_0atl?sslmode=require
spring.datasource.username=todoapp_db_0atl_user
spring.datasource.password=bwHlVhxRUOAS0qhdb5HfHuuSXIU0LqfQ

# JPA Configuration
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# Connection Pool
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.max-lifetime=1800000

# Server Configuration
server.port=8080
server.address=0.0.0.0

# JWT Secret (use environment variable in production)
jwt.secret=5367566B59703373367639792F423F4528482B4D6251655468576D5A71347437

# Security
spring.security.oauth2.resourceserver.jwt.secret-key=${jwt.secret}

# Admin Registration Key
admin.registration.key=123456

# Production logging
logging.level.root=INFO
logging.level.com.examly.springapp=DEBUG
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n

# SSL for PostgreSQL
spring.datasource.hikari.data-source-properties.sslmode=require
spring.datasource.hikari.data-source-properties.ssl=true
spring.datasource.hikari.data-source-properties.sslfactory=org.postgresql.ssl.DefaultSSLFactory